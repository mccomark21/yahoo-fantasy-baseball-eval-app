# Triage labels

The engineering skills use five canonical triage roles. This project currently maps each role directly to the same label string.

| Skill role | Repository label | Meaning |
| --- | --- | --- |
| needs-triage | needs-triage | Maintainer needs to evaluate this issue |
| needs-info | needs-info | Waiting on reporter for more information |
| ready-for-agent | ready-for-agent | Fully specified and ready for an agent |
| ready-for-human | ready-for-human | Requires human implementation |
| wontfix | wontfix | Will not be actioned |

If repository labels change later, update only the Repository label column.
